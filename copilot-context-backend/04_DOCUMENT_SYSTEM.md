# Document System

- Document types are configurable
- Each document has validation rules (JSON config)
- A service may require multiple document types
- Each document type may require multiple files
- Documents must be reviewed and approved manually

No document requirement is hardcoded.
